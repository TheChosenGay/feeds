[project]
name = "feeds-workers"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "confluent-kafka>=2.3.0",
    "redis>=5.0.0",
    "psycopg[binary]>=3.1.0",
]

[tool.ruff]
line-length = 100
